formula(){

    int someNumber = petAssessment();
    
    String type;
    
    boolean panting;
    boolean gums;
    boolean heartProblem;
    boolean tongue;
    boolean diarrhea;
    boolean vomit;

    int thresholdLower;
    int thresholdUpper;


    if(type == "cat"){

        if( panting || gums || heartrate || tongue || diarrhea || vomit ){
            highRisk();

        }else if( thresholdUpper > someNumber > thresholdLower){
            midRisk();

        }else{
            lowRisk();
        }   

    }else{

        if( gums || heartrate || tongue || diarrhea || vomit ){
            highRisk();

        }else if( thresholdUpper > number > thresholdLower){
            midRisk();

        }else{
            lowRisk();
        }   
    }
}


petAssessment(){
    String coatType;
    int temperature;

    int value = catDog();

    int assessmentNumber = 0;

    if( temperature < 0 ){
        if( coatType == "long" ){
            no cold risk multiplier
            assessmentNumber = value;
            return value;

        }
        else{
            cold risk multiplier * value;
            assessmentNumber = value;
            return assessmentNumber
        }
    }
    else if( 1 < temperature < 29 ){
        if( coatType == "long" ){
            multiplier??

            assessmentNumber = value;
            return value;

        }
        else{
            multiplier ??

            assessmentNumber = value;
            return assessmentNumber
        }

    }
    else{
        if( coatType == "long" ){
            heat risk multiplier * value;
            assessmentNumber = value;
            return assessmentNumber;

        }
        else{
            no heat risk multiplier;
            assessmentNumber = value;
            return assessmentNumber
        }

    }

return assessmentNumber;

}


catDog(){

    String type;  //Cat or dog
    int petStats = 0;

    if( type == "cat" ){

        petStats += petAge();

        petStats += petWeight();

        petStats += petPreConditons();

        petStats += petSymptoms();
        
    }else{

        petStats += petAge();

        petStats += petWeight();

        petStats += petPreConditons();

        petStats += petSymptoms();
    }   

    return petStats;
}


petAge(){

    int age
    int ageValue = 0;

        if( age <= 2 ){
            ageValue +-

        }else if( 10 >= age >= 3){
            ageValue +-

        }else{
            ageValue +-
        }

    return ageValue
}

petWeight(){

    String size;
    weightValue = 0;

        if(size == "thin"){
            weightValue +-

        }else if(size == "normal"){
            weightValue +-

        }else if(size == "overweight"){
            weightValue +-

        }else{
            weightValue +-

        }
    
    return weightValue
}

petPreConditons(){

    boolean flatFace;
    boolean brainProb;
    boolean heartProb;
    boolean lungProb;

    preConditonsValue = 0;

        if(flatFace){
            preConditonsValue +-
        }
        if(brainProb){
            preConditonsValue +-
        }
        if(heartProb){
            preConditonsValue +-
        }
        if(lungProb){
            preConditonsValue +- 
        }

    return preConditonsValue;
}

petSymptoms(){

    boolean drool;
    boolean agitated;
    boolean dizziness;
    boolean lethargy;

    symptomsValue = 0;

        if(drool){
            symptomsValue +- 
        }
        if(agitated){
            symptomsValue +- 
        }
        if(dizziness){
            symptomsValue +-
        }
        if(lethargy){
            symptomsValue +-
        }

    return symptomsValue;
}


